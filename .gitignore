logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.env

# Forge desktop app working files (canvas state, chat attachments)
.forge

# Forge's local dev sidecar installs with pnpm; production (Vercel) builds with npm + package-lock.json.
# Keep the pnpm files out of the repo so the live build toolchain does not change.
pnpm-lock.yaml
pnpm-workspace.yaml
